# macOS
.DS_Store
._*
.Spotlight-V100
.Trashes

# Éditeurs
.vscode/
.idea/
*.swp

# Dépendances / build (pour plus tard : Tailwind, etc.)
node_modules/
dist/
.env
.env.*

# Captures d'écran / harnais de test (hors site)
Capture d’écran*.png
_captest.html

# Skill Claude déposé dans le projet (gardé en local, hors dépôt)
skill/

# Fichiers temporaires d'écriture des JSON
data/*.tmp
data/**/*.tmp

# Mot de passe admin (hash) : généré par scripts/seed.php, ne pas versionner
data/auth.json
